trigger:
- master

pool:
  vmImage: 'windows-2019' # AzureFileCopy task only works on Windows

strategy:
  matrix:
    default:
      name: 'default'

steps:
- task: AzureFileCopy@3
  inputs:
    SourcePath: '$(name)'
    azureSubscription: 'DCD-CFTAPPS-SBOX'
    Destination: 'AzureBlob'
    storage: 'st$(name)shuttersbox'
    ContainerName: '$web'
# - task: AzureFileCopy@3
#   inputs:
#     SourcePath: '$(name)'
#     azureSubscription: 'DCD-CFTAPPS-PROD'
#     Destination: 'AzureBlob'
#     storage: 'st$(name)shutterprod'
#     ContainerName: '$web'
