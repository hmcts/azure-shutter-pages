name: Deploy OPAL page to opal-rm-frontend Production shutter app
run-name: ${{ github.ref_name }} ${{ github.actor }}

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - 'opal/**'
      - '.github/workflows/opal-rm-frontend-prod.yaml'
  pull_request:
    types: [opened]
    branches:
      - master
    paths:
      - 'opal/**'
      - '.github/workflows/opal-rm-frontend-prod.yaml'

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    uses: hmcts/azure-shutter-pages/.github/workflows/deploy-shutter-pages.yaml@master
    with:
      shutter_page_folder: opal
    secrets:
      WEBAPP_TOKEN: ${{ secrets.OPAL_RM_FRONTEND_PROD }}
