name: push shutter page to et3-pet static webapp
run-name: ${{ github.ref_name }} ${{ github.actor }}

on:
  workflow_dispatch:
  check_run:
    types:
      - completed
  push:
    branches:
      - master
    paths:
      - 'et3-pet/**'
      - .github/workflows/et3-pet.yaml

  pull_request:
    types: [opened]
    branches:
      - master
    paths:
      - 'et3-pet/**'
      - .github/workflows/et3-pet.yaml

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    uses: hmcts/azure-shutter-pages/.github/workflows/deploy-shutter-pages.yaml@master
    with:
      shutter_page_folder: "et3-pet" # App source code path relative to repository root. Enter "default" for default pages.
    secrets:
      WEBAPP_TOKEN: ${{ secrets.ET_STAFF_PET_PROD }}
